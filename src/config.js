// eslint-disable-next-line no-unused-vars
const host = 'http://106.13.118.19:5757'

const config = {
  host,
  loginUrl: `${host}/weapp/login`,
  userUrl: `${host}/weapp/user`
}

export default config
